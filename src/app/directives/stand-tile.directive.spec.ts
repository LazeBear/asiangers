import { StandTileDirective } from './stand-tile.directive';

describe('StandTileDirective', () => {
  it('should create an instance', () => {
    const directive = new StandTileDirective();
    expect(directive).toBeTruthy();
  });
});
