import { FenceTileDirective } from './fence-tile.directive';

describe('FenceTileDirective', () => {
  it('should create an instance', () => {
    const directive = new FenceTileDirective();
    expect(directive).toBeTruthy();
  });
});
