import { LongFenceDirective } from './long-fence.directive';

describe('LongFenceDirective', () => {
  it('should create an instance', () => {
    const directive = new LongFenceDirective();
    expect(directive).toBeTruthy();
  });
});
