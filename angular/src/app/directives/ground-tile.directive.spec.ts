import { GroundTileDirective } from './ground-tile.directive';

describe('GroundTileDirective', () => {
  it('should create an instance', () => {
    const directive = new GroundTileDirective();
    expect(directive).toBeTruthy();
  });
});
